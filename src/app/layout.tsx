import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Manee Coffee Roasters - Sustainably Sourced Coffee from Laos',
  description: 'Discover the unique flavors of sustainably sourced coffee beans from the beautiful highlands of Laos. Premium coffee roasted with care.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}